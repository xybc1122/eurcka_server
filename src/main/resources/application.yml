server:
  port: 8660

eureka:
  instance:
    hostname: 127.0.0.1
  client:
  #�����Լ��Ǹ������
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
